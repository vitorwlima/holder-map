import styled from 'styled-components'

export const Container = styled.main`
  padding: 4rem;

  header {
    display: flex;
    align-items: center;
    justify-content: space-between;

    h1 {
      font-size: 2rem;
      margin-bottom: 1rem;
    }
  }
`
